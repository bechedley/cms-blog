<div id="posts">
    <div class="row mx-0">
        {{#each posts as |post|}}
        <div class="post-div col-lg-4 col-md-6" data-id="{{post.id}}">
            <div class="card d-flex flex-column align-items-center">
                <div class="post-title">
                    <h3>{{post.post_title}}</h3>
                </div>
                <div class="post-title">
                    <p>{{ format_date post.post_created}}</p>
                </div>
                <div class="card-img">
                    <br />
                    <p class="text-center">{{post.post-text}}</p>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>